import React, { useEffect } from 'react'
import { useCart } from '../contexts/CartProvider';

const ChangeQuantity = () => {
    // const { quantity, handleQuantityChange } = useCart();

    return (
        <div></div>
    )
}

export default ChangeQuantity
